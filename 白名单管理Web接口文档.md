白名单管理Web接口

> 用于管理网关白名单规则。白名单支持以下IP匹配模式：
> - **精确匹配**：`192.168.1.100`
> - **通配符匹配**：`192.168.1.*`（匹配整个C段）
> - **范围匹配**：`192.168.1.1-192.168.1.254`（匹配指定区间）

## 1. 添加白名单

请求接口
```http request
POST http://<IP>:<PORT>/gateway/whitelist/add
Content-Type: application/json

{
    "ipAddr": "192.168.1.100",
    "macAddr": "AA:BB:CC:DD:EE:01"
}
```

| 序号 | 参数    | 描述     | 类型   | 说明                                                         |
| ---- | ------- | -------- | ------ | ------------------------------------------------------------ |
| 1    | ipAddr  | IP地址   | 字符串 | 必填，支持精确值`1.1.1.1`、通配符`1.1.1.*`、范围`1.1.1.1-1.1.1.254` |
| 2    | macAddr | MAC地址  | 字符串 | 必填                                                         |

返回结构

- 成功样例
```json
{
    "msg": "成功",
    "code": 200
}
```

- 失败样例
```json
{
    "msg": "添加失败",
    "code": 701
}
```

## 2. 删除白名单

请求接口
```http request
POST http://<IP>:<PORT>/gateway/whitelist/del?id=<param>
Content-Type: application/x-www-form-urlencoded
```

| 序号 | 参数 | 描述     | 类型 | 说明 |
| ---- | ---- | -------- | ---- | ---- |
| 1    | id   | 白名单id | 数字 | 必填 |

返回结构

- 成功样例
```json
{
    "msg": "成功",
    "code": 200
}
```

- 失败样例
```json
{
    "msg": "删除失败",
    "code": 702
}
```

## 3. 修改白名单

请求接口
```http request
POST http://<IP>:<PORT>/gateway/whitelist/update
Content-Type: application/json

{
    "id": 1,
    "ipAddr": "192.168.1.*",
    "macAddr": "AA:BB:CC:DD:EE:01"
}
```

| 序号 | 参数    | 描述     | 类型   | 说明                                                         |
| ---- | ------- | -------- | ------ | ------------------------------------------------------------ |
| 1    | id      | 白名单id | 数字   | 必填                                                         |
| 2    | ipAddr  | IP地址   | 字符串 | 选填，支持精确值`1.1.1.1`、通配符`1.1.1.*`、范围`1.1.1.1-1.1.1.254` |
| 3    | macAddr | MAC地址  | 字符串 | 选填                                                         |

返回结构

- 成功样例
```json
{
    "msg": "成功",
    "code": 200
}
```

- 失败样例
```json
{
    "msg": "修改失败",
    "code": 703
}
```

## 4. 查询白名单列表

请求接口
```http request
GET http://<IP>:<PORT>/gateway/whitelist/list
```

返回结构

- 成功样例
```json
{
    "data": [
        {
            "id": 1,
            "ipAddr": "192.168.1.100",
            "macAddr": "AA:BB:CC:DD:EE:01",
            "createTime": "2026-06-23 14:00:00",
            "updateTime": "2026-06-23 14:00:00"
        },
        {
            "id": 2,
            "ipAddr": "192.168.1.*",
            "macAddr": "AA:BB:CC:DD:EE:02",
            "createTime": "2026-06-23 14:05:00",
            "updateTime": "2026-06-23 14:05:00"
        },
        {
            "id": 3,
            "ipAddr": "10.0.0.1-10.0.0.100",
            "macAddr": "AA:BB:CC:DD:EE:03",
            "createTime": "2026-06-23 14:10:00",
            "updateTime": "2026-06-23 14:10:00"
        }
    ],
    "msg": "成功",
    "code": 200
}
```

| 序号 | 参数       | 描述       | 类型   | 说明                                                         |
| ---- | ---------- | ---------- | ------ | ------------------------------------------------------------ |
| 1    | id         | 白名单id   | 数字   |                                                              |
| 2    | ipAddr     | IP地址     | 字符串 | 精确值、通配符`*`或范围`-`格式                               |
| 3    | macAddr    | MAC地址    | 字符串 |                                                              |
| 4    | createTime | 创建时间   | 字符串 |                                                              |
| 5    | updateTime | 更新时间   | 字符串 |                                                              |

- 失败样例
```json
{
    "msg": "查询失败",
    "code": 704
}
```

## 5. 根据IP和MAC地址查询白名单

请求接口
```http request
GET http://<IP>:<PORT>/gateway/whitelist/query?ip=<param1>&macAddr=<param2>
```

| 序号 | 参数    | 描述     | 类型   | 说明                         |
| ---- | ------- | -------- | ------ | ---------------------------- |
| 1    | ip      | IP地址   | 字符串 | 必填，仅支持精确值           |
| 2    | macAddr | MAC地址  | 字符串 | 必填                         |

> 说明：该接口用于登录校验场景，`ip`传实际客户端IP（精确值），服务端自动与表中存储的IP模式（精确值、通配符、范围）进行匹配。例如表中存储`192.168.1.*`，传入`192.168.1.50`可匹配成功。

返回结构

- 成功样例（表中存储`192.168.1.*`，查询`192.168.1.50`）
```json
{
    "data": {
        "id": 2,
        "ipAddr": "192.168.1.*",
        "macAddr": "AA:BB:CC:DD:EE:02",
        "createTime": "2026-06-23 14:05:00",
        "updateTime": "2026-06-23 14:05:00"
    },
    "msg": "成功",
    "code": 200
}
```

- 失败样例（未匹配到白名单规则）
```json
{
    "msg": "查询失败",
    "code": 704
}
```

## 测试数据示例

以下数据供开发和测试使用：

```sql
-- 插入测试数据
INSERT INTO gateway_whitelist (ip_addr, mac_addr, create_time, update_time)
VALUES
('192.168.1.100',    'AA:BB:CC:DD:EE:01', '2026-06-23 14:00:00', '2026-06-23 14:00:00'),  -- 精确匹配
('192.168.1.*',      'AA:BB:CC:DD:EE:02', '2026-06-23 14:05:00', '2026-06-23 14:05:00'),  -- 通配符匹配
('10.0.0.1-10.0.0.100', 'AA:BB:CC:DD:EE:03', '2026-06-23 14:10:00', '2026-06-23 14:10:00'),  -- 范围匹配
('172.16.0.*',       'AA:BB:CC:DD:EE:04', '2026-06-23 14:15:00', '2026-06-23 14:15:00'),  -- 通配符匹配
('192.168.0.1-192.168.0.50', 'AA:BB:CC:DD:EE:05', '2026-06-23 14:20:00', '2026-06-23 14:20:00');  -- 范围匹配
```

### 测试场景对照表

| 测试数据 (ipAddr)                | 查询IP         | macAddr             | 是否匹配 | 说明             |
| -------------------------------- | -------------- | ------------------- | -------- | ---------------- |
| `192.168.1.100`                  | `192.168.1.100` | `AA:BB:CC:DD:EE:01` | 是       | 精确匹配         |
| `192.168.1.100`                  | `192.168.1.101` | `AA:BB:CC:DD:EE:01` | 否       | IP不匹配         |
| `192.168.1.*`                    | `192.168.1.50`  | `AA:BB:CC:DD:EE:02` | 是       | 通配符匹配       |
| `192.168.1.*`                    | `192.168.2.1`   | `AA:BB:CC:DD:EE:02` | 否       | C段不匹配        |
| `10.0.0.1-10.0.0.100`           | `10.0.0.50`     | `AA:BB:CC:DD:EE:03` | 是       | 范围匹配         |
| `10.0.0.1-10.0.0.100`           | `10.0.0.200`    | `AA:BB:CC:DD:EE:03` | 否       | 超出范围         |
| `172.16.0.*`                     | `172.16.0.88`   | `AA:BB:CC:DD:EE:04` | 是       | 通配符匹配       |
| `192.168.0.1-192.168.0.50`      | `192.168.0.25`  | `AA:BB:CC:DD:EE:05` | 是       | 范围匹配         |
